<Project Sdk="Microsoft.NET.Sdk">

  <!-- EchoFactory.Content: JSON loading, validation, registries.
       Depends only on Core. System.Text.Json is in-box for net8.0.
       (No TreatWarningsAsErrors here: JSON DTOs intentionally use mutable
       collection properties, which fights some CA analyzers. Core stays strict.) -->
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\EchoFactory.Core\EchoFactory.Core.csproj" />
  </ItemGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="EchoFactory.Content.Tests" />
  </ItemGroup>

</Project>
